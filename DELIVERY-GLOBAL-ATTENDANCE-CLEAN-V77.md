# MZJ Platform — Global Attendance CLEAN V77

## الهدف
تأمين الانتقال إلى نظام الحضور العام بدون تعطيل دخول الموظفين أثناء تجهيز الجداول، مع إصلاح فشل الـDeployment في V76.

## الوضع الآمن
- يوجد إعداد مركزي جديد: **تطبيق الحضور على تسجيل الدخول**.
- القيمة الافتراضية بعد النشر: **متوقف**.
- أثناء التوقف:
  - كل اليوزرات يستطيعون تسجيل الدخول عادي.
  - وجود جدول عمل معيّن لليوزر لا يمنع الدخول خارج الفترة.
  - فحص الجلسة لا يخرج المستخدم بسبب الحضور.
  - Attendance Cron لا ينفذ Auto Checkout أو Forced Logout.
  - تعديل جدول/فرع/فترة/مكان/يوم إجازة لليوزر لا ينهي جلسته الحالية.
- عند تفعيل الإلزام من مدير النظام:
  - يتم حفظ الإعداد مركزيًا في PostgreSQL.
  - يتم إنهاء جلسات اليوزرات الذين لديهم تعيينات عمل حالية مرة واحدة حتى يسجلوا الدخول من جديد عبر دورة الحضور.
  - بعد ذلك يعمل تسجيل الحضور، فحص الفترة، اللوكيشن، Auto Checkout وForced Logout حسب الجدول المعيّن لكل User.

## إصلاح Deployment V76
سبب الفشل الظاهر كان فحصًا قديمًا يتطلب baseline `1.19.16` بينما V76 كانت تحمل `1.19.18`، لذلك كان الفحص:
`Marketing packages + MZJ Club v45 checks: 15/16 passed`
ثم يفشل `pnpm run build`.

تمت إعادة package baseline إلى `1.19.16` بدون تغيير منطق الأنظمة الأخرى، وأصبح الفحص `16/16 passed`.

## التحقق
- `Marketing packages + MZJ Club v45`: **16/16 PASS**.
- `Global attendance v77`: **28/28 PASS**.
- API/server import extension check: PASS.
- Merge conflict check: PASS.
- TypeScript syntax transpile للملفات `src/server/api`: **267/267 PASS**.
- لم يتم تنفيذ Full dependency build داخل بيئة التسليم بسبب عدم توفر dependencies المحلية وعدم توفر registry network؛ Vercel سيشغّل `pnpm install --frozen-lockfile` قبل الـbuild كالمعتاد.

## تشغيل النظام بعد النشر
1. اترك **تطبيق الحضور على تسجيل الدخول** متوقفًا أثناء تجهيز المستخدمين.
2. اضبط الجداول والفترات والفرع والمكان المطلوب ويوم الإجازة لكل User.
3. بعد التأكد، فعّل الإلزام من إعدادات الحضور والانصراف.
