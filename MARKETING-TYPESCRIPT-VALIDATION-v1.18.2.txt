MZJ Marketing Native Rebuild - TypeScript Validation v1.18.2
================================================================

Canonical source corrections
----------------------------
1. MarketingLayout uses one non-null MarketingMeta instance after the loading/error guard.
2. MarketingDashboardPage uses the supported Archive icon export.
3. DashboardCampaign replaces the generic departments projection instead of intersecting two incompatible department shapes.
4. MarketingSettingsPanel normalizes campaign types, creatives, funnels, content sections, and order statuses into one explicit CatalogItemRow view model.
5. Owner-color settings are runtime-validated before entering Record<string, string> state.
6. The marketing structural checker verifies the same canonical permission-access expression used by the source.

Validation performed
--------------------
PASS - Marketing frontend strict TypeScript validation with TypeScript 5.8.3.
PASS - Marketing server strict TypeScript validation with TypeScript 5.8.3.
PASS - CRM static and architecture checks.
PASS - Operations static, workflow, approval, archive, and vehicle-management checks.
PASS - Tracking static and integration checks.
PASS - Worker route checks.
PASS - Marketing native rebuild structure and exclusion checks.
PASS - Marketing canonical migration structure checks.

Environment limitation
----------------------
The execution environment could not reach the npm registry, so pnpm install and the real Vite bundle were not run locally. Temporary dependency-compatible declarations were used only for strict TypeScript validation and are not included in this source archive.

Database
--------
No new migration is introduced in v1.18.2. If the v1.18.1 canonical SQL completed successfully, do not run it again for this TypeScript-only source correction.
