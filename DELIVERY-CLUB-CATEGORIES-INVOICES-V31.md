# MZJ Platform - MZJ Club Categories & NEXT ERP Invoices - v31

نسخة سورس كاملة مبنية مباشرة من MZJ Platform v30. لا تحتوي على Patch أو Diff أو ترقية فوق نسخة معدلة سابقة.

## MZJ Club Community

- تغيير عمود `النوع` إلى `الفئة` في `عملاء تم البيع` و`العملاء الجديدة`.
- الفئات الثابتة حسب إجمالي النقاط المكتسبة طوال رحلة العميل (`lifetime_points`):
  - تميز: 1000 إلى 1499.
  - ذهبي: 1500 إلى 2499.
  - خاصة: 2500 فأعلى.
- الفئة لا تنخفض عند استبدال النقاط لأن الاستبدال يخصم من الرصيد المتاح فقط ولا يخصم من إجمالي النقاط المكتسبة.
- فلتر مستقل بالفئة في التبويبين.
- Badge بلون مستقل لكل فئة.

## بطاقة العضوية

- فئة العميل تظهر في ظهر البطاقة.
- ظهر البطاقة يعرض أيضًا: كود العميل، تاريخ الانضمام، عدد مرات الشراء، وآخر تاريخ شراء.
- لا يوجد QR لصفحة العضوية.
- كتالوج المكافآت منفصل عن بطاقة العضوية ولا يتطلب اختيار مكافآت لإظهارها في البطاقة.

## سجل الحركة والمشتريات

- حركة `شراء العميل` و`إعادة الشراء` تعرض السيارة/السيارات الفعلية المرتبطة بنفس Sales Order من `tracking.order_vehicles`.
- الربط يعتمد على Sales Order المرجعي المحفوظ مع عملية الشراء، ولا يعتمد على تخمين اسم السيارة.

## فاتورة NEXT ERP

- زر `تحميل الفاتورة` يظهر مع حركة الشراء عندما يوجد Sales Order مرجعي.
- المنصة تتحقق أولًا أن Sales Order يخص نفس عضوية العميل.
- يتم البحث Server-to-Server في NEXT ERP عن `Sales Invoice Item.sales_order` المطابق لنفس Sales Order.
- يتم قبول Sales Invoice Submitted فقط واستبعاد الفواتير الملغاة.
- عند وجود فاتورة واحدة يتم تحميل PDF مباشرة من Print API في NEXT ERP.
- عند وجود أكثر من فاتورة يتم عرض كل الفواتير المرتبطة ليختار العميل منها.
- إذا لم توجد فاتورة تظهر رسالة `الفاتورة غير متاحة بعد` ولا يتم إنشاء PDF بديل.

## متغيرات البيئة المطلوبة للفواتير

- `NEXT_ERP_URL=https://mzj2.newworldinfosys.com`
- `NEXT_API_KEY`
- `NEXT_API_SECRET`
- `NEXT_ERP_SALES_INVOICE_PRINT_FORMAT` اختياري، والقيمة الافتراضية `Standard`.

المفاتيح تبقى في بيئة السيرفر/Vercel ولا ترسل إلى المتصفح.

## التحقق

- MZJ Owners Community baseline checks: 83/83.
- MZJ Club v31 focused checks: 21/21.
- JavaScript/TypeScript syntax transpile checks نجحت لكل الملفات المعدلة والجديدة.
- فحص merge-conflict markers نجح.
- لم يتم تنفيذ اختبار LIVE على NEXT ERP من بيئة الإنتاج؛ تحميل الفاتورة يحتاج متغيرات البيئة السابقة وصلاحية قراءة Sales Invoice/Sales Invoice Item وPrint PDF.
