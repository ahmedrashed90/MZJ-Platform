# MZJ Platform v1.21.4 — Cash QR Contacts

النطاق: تسجيل QR مبيعات الكاش فقط.

- عند نجاح تسجيل عميل جديد من `/cash-register` يتم إنشاء أو إعادة استخدام سجل واحد في `crm.contacts` اعتمادًا على رقم الجوال الموحّد.
- يتم ربط `crm.leads.contact_id` بنفس جهة الاتصال، لذلك يظهر العميل في صفحة CRM > جهات الاتصال مباشرة حسب صلاحيات المستخدم.
- يتم إنشاء هوية `cash_qr` في `crm.contact_identities` لربط مصدر التسجيل بجهة الاتصال.
- منع تكرار العميل في `crm.leads` بنفس رقم الجوال لم يتغير.
- التوزيع، الفرع، المصدر، الدفع، الحالة، القسم، المسؤول، التواريخ، وكود العميل القديم في MZJ Owners لم تتغير.
- لا توجد Migration أو Patch منفصلة؛ النسخة Full مبنية على v1.21.3 كاملة.
