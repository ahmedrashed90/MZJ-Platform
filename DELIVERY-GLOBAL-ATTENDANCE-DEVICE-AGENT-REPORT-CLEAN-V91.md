# MZJ Platform — Device Agent + Attendance Report CLEAN v91

## الأساس
- مبني مباشرة على `MZJ-Platform-DEVICE-AGENT-ATTENDANCE-REPORT-CLEAN-v90`.
- لم يتم تغيير منطق Device Agent أو Login أو الحضور والانصراف أو الصلاحيات.
- لا يوجد GPS أو Public IP أو Geofence.
- التعديل مدمج في السورس الأساسي بدون Patch أو Hotfix منفصل.

## تعديلات صفحة تقارير الحضور والانصراف
- إزالة النص: `تقرير مركزي للحضور والانصراف حسب الفترات الفعلية المحددة لكل موظف.`
- إزالة عنوان `تقارير الحضور والانصراف` المكرر داخل الكارت، والإبقاء على عنوان الصفحة مرة واحدة فقط.
- كل يوم يظهر داخل Block مستقل.
- عنوان اليوم يحتوي فقط على اليوم والتاريخ.
- الضغط على عنوان أي يوم يفتح/يقفل تفاصيل هذا اليوم بشكل مستقل.
- عند تحميل تقرير جديد تفتح الأيام من جديد.
- ألوان الأيام أصبحت 8 درجات بصرية مختلفة بدل لونين متناوبين.
- النتيجة تظل تعرض عدد دقائق التأخير؛ التأخير باللون الأحمر وصفر دقيقة باللون الأخضر.
- لا يوجد تحديث تلقائي كل 10 ثوانٍ.

## تصدير Excel
- زر `تصدير Excel` يعيد طلب البيانات باستخدام الفلاتر الحالية لحظة الضغط:
  - من تاريخ.
  - إلى تاريخ.
  - الموظف/الموظفون المحددون.
- التصدير لا يعتمد على حالة فتح/قفل Blocks في الشاشة؛ يتم تصدير كل نتائج الفلاتر.
- إذا تم تغيير الفلاتر ولم يتم الضغط على `عرض` قبل التصدير، ملف Excel يستخدم الفلاتر الجديدة مباشرة.
- إذا لم توجد نتائج للفلاتر الحالية لا يتم إنشاء ملف فارغ وتظهر رسالة بعدم وجود نتائج.

## Device Agent
- منطق v90 محفوظ كما هو:
  - Required / Exempt لكل مستخدم.
  - Pending / Approve / Revoke.
  - Challenge + ECDSA Signature.
  - Windows DPAPI.
  - جهاز Approved واحد نشط للمستخدم المطلوب منه التحقق.
  - المستخدم المستثنى يدخل بدون Agent.

## التحقق
- Attendance + Device Agent + Report v91: `34/34 PASS`.
- Marketing Packages + MZJ Club baseline: `16/16 PASS`.
- API import extension check: PASS.
- Merge-conflict check: PASS.
- TypeScript/TSX syntax transpile: `270/270 PASS`.
- CSS braces: PASS.
