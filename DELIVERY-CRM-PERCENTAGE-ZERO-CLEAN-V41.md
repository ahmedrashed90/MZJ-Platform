# MZJ Platform — CRM Percentage Zero CLEAN v41

- Baseline: full v40 source.
- Percentage distribution accepts each selected agent percentage from 0 through 100 inclusive.
- 0% keeps the agent in the rule but excludes that agent from percentage routing.
- Per-branch total must still equal 100%.
- Percentage routing never falls back to round-robin merely because one or more agents are configured at 0%.
- No patch/diff artifact is included.
