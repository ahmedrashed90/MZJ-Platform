# الملفات المعدلة — v1.20.2

1. `src/marketing/components/TaskDetailModal.tsx`
   - مراجعة الحقول باللون الأصفر.
   - ملاحظات منفصلة لكل حقل.
   - عرض الملاحظات للمستخدم المكلّف.
   - إعادة تصميم أزرار المراجعة.
   - توافق الملاحظات القديمة.

2. `src/marketing/pages/MarketingDashboardPage.tsx`
   - تجميع `TASK - المطلوب` حسب القسم.
   - تجميع `جاهزية المطلوب` حسب الحملة والقسم.
   - كارت تاسك موحد بالمعلومات المطلوبة.
   - الحفاظ على فلو الاستلام الحالي.

3. `src/marketing/marketing.css`
   - تنسيق شاشة المراجعة والحقول المحددة والملاحظات.
   - شريط إجراءات المراجعة.
   - تنسيق كروت الداش بورد والأقسام.

4. `server/marketing/index.ts`
   - إرجاع `canViewFeedback` للمستخدم المكلّف أو صاحب الصلاحية.
   - لا تغيير في حالات المراجعة أو فلو الاستلام أو قاعدة البيانات.

5. `scripts/check-marketing-review-workflow-v1202.mjs`
   - فحص رجوع مخصص للتعديلات الجديدة.

6. `package.json`
   - إضافة فحص v1.20.2 إلى بداية أمر `typecheck`.

7. ملفات التوثيق ونتائج الاختبارات الخاصة بالتسليم.
