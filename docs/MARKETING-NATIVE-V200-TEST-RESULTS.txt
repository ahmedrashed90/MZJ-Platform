MZJ Marketing Native Rebuild v2.0 - Test Results
Date: 2026-07-23

PASSED
1. Marketing architecture validation:
   node scripts/check-marketing-native-v200.mjs
   Result: PASS
   - Required pages, routes, API actions, schema tables and operations photography integration verified.
   - No explicit any/as any in the new marketing source.
   - No Firebase runtime, iframe, Local Publisher, Publisher Agent or Marketing Checklist in the new marketing source.

2. Marketing client TypeScript focused check:
   tsc -p /tmp/tsconfig-marketing.json --pretty false
   Result: PASS
   Notes: TypeScript 5.8.3 was run with temporary ambient declarations because project dependencies could not be downloaded in the isolated build environment. No temporary declarations were copied into the delivered source.

3. Marketing server TypeScript focused check:
   tsc -p /tmp/tsconfig-server-marketing.json --pretty false
   Result: PASS

4. Operations shared photography request TypeScript focused check:
   tsc -p /tmp/tsconfig-server-operations.json --pretty false
   Result: PASS

5. Existing platform static regression checks:
   Result: PASS
   - API imports
   - CRM structure and reference checks
   - Tracking module checks
   - Operations Native V2 and workflow checks
   - ERPNext/Tracking integration checks
   - Marketing native v2.0 check

6. Source safety checks:
   Result: PASS
   - Task numbers are generated by the server and constrained unique in PostgreSQL.
   - Campaign/agenda creation is transactional and idempotent.
   - Template tasks are unique per writer and Creative Instance.
   - Execution tasks are unique per executive x writer x Creative Instance relation.
   - Calendar reads received_at (actual receipt time).
   - Photography requests use one shared operations record.

NOT EXECUTED IN THIS ENVIRONMENT
1. pnpm install --frozen-lockfile
   Result: BLOCKED
   Reason: registry.npmjs.org DNS resolution failed with EAI_AGAIN while Corepack attempted to obtain pnpm 9.15.9.

2. Full pnpm run typecheck / pnpm run build
   Result: BLOCKED BY DEPENDENCY INSTALLATION
   No build error from the project source was observed; the command could not start because dependencies were unavailable in this environment.

3. Live PostgreSQL API/integration tests
   Result: NOT EXECUTED
   Reason: no DATABASE_URL or test PostgreSQL service was available in the execution environment.

4. Live R2 upload tests and RAW server folder creation
   Result: NOT EXECUTED
   Reason: no R2 credentials or MZJ_RAW_API_URL were available.
