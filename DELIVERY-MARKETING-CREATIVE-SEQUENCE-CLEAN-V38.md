# MZJ Platform — Marketing Creative Sequence CLEAN v38

- المصدر: سورس v37 الكامل.
- إصلاح إنشاء كرييتيف من قاعدة بيانات التسويق بعد حذف كرييتيف سابق.
- أُلغي الاعتماد على `COUNT + 1` و `COUNT + 1000` في توليد أرقام الكرييتيف وTask Template.
- أضيفت حالة تسلسل دائمة لكل حملة/أجندة داخل `marketing.entity_sequences`.
- رقم الكرييتيف الجديد لا يعاد استخدامه بعد الحذف؛ التهيئة تراعي الكرييتيفات الحالية وسجل `creative_deleted` السابق.
- كل دفعة Task Templates تحصل على رقم batch متزايد وآمن، مع بقاء `task_no` فريدًا.
- نفس آلية التخصيص مستخدمة في إنشاء الحملات، الأجندات، والإضافة من صفحة قاعدة البيانات.
- لا يوجد Patch/Diff؛ الحزمة Full Clean مستقلة.
